name: Integración y entrega continua

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      nombres: "Daniel"
      apellidos: "Huamani"
      edad: "25"
      version_proyecto: "3.0"

    steps:
      - name: presentación
        run: |
          echo "Nombres: $nombres"
          echo "Apellidos: $apellidos"
          echo "Edad: $edad"

      - name: datos generales
        run: echo "carrera de ingeniería de sistemas"

      - name: variables de entorno secretas
        env:
          user: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
        run: |
          echo "user: *****"
          echo "password: *****"

      - name: variables abiertas
        run: echo "Versión del proyecto: $version_proyecto"
